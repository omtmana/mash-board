// Packages / Modules
const express = require('express')
const app = express()

// Routes


// Start server
const port = 4000
app.listen(port, () => {
   console.log(`Server running on port: ${port}`)
})